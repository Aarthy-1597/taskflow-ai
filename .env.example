# Node.js API - projects, tasks, users, /api/auth/me (optional)
# Local:  VITE_API_URL=http://localhost:3000
# Render: VITE_API_URL=https://hackathon-7-v2y5.onrender.com
# VITE_API_URL=http://localhost:3000

# Python Time Tracking API (port 8000)
# Leave empty to use Vite proxy (dev) or same-origin. Set for direct URL (e.g. production).
VITE_TIME_API_URL=http://127.0.0.1:8000

# Auth API - uses VITE_API_URL if not set. Set only when auth is on a different host.
# VITE_AUTH_API_URL=http://localhost:3000

# Optional: Bearer token for dev/testing when backend expects token auth (e.g. cross-origin)
# VITE_AUTH_TOKEN=your_jwt_token_here
